"""ATUM Audit Agent — Cryptographic integrity tracking with OWL ontology + EU AI Act compliance."""
import logging

__version__ = "2.0.0"

logging.getLogger("atum_audit").addHandler(logging.NullHandler())
